apiVersion: v2
name: cert-manager-webhook-easydns
description: |
  A cert-manager DNS-01 webhook solver for easyDNS.
  Enables automatic TLS certificate issuance (including wildcards) via
  the easyDNS REST API.
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - cert-manager
  - easydns
  - acme
  - dns01
  - tls
  - certificates
